import variables

# Home location
home = (0,0,0)
home_size = (variables.dim_x / 10, variables.dim_y / 10)

# This is the number of steps a robot spends looking for a suitable beam 
# to climb on before it creates its own structure
steps_to_start = 10